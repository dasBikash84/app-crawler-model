package com.dasBikash.app_crawler_model

import androidx.annotation.Keep

/**
 * Each enum type corresponds to Http request type of the same name
 * */
@Keep
enum class HttpMethod{GET,POST,PUT,PATCH,DELETE}